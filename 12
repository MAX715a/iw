-- ============================================================
-- 👑 MAX ULTRA LUXURY ENGINE (Universal 2026 Edition)
-- 🌐 Full GitHub Production Code (PC & Mobile Adaptive)
-- ============================================================

local MAX_UI = {}
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local Stats = game:GetService("Stats")

local LocalPlayer = Players.LocalPlayer
local IsMobile = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled
local DeviceType = IsMobile and "Mobile 📱" or "PC 🖥️"

-- 🔓 فك قفل الفريمات تلقائياً
pcall(function()
    if setfpscap then setfpscap(240)
    elseif set_fps_cap then set_fps_cap(240) end
end)

local Theme = {
    Background = Color3.fromRGB(12, 13, 17),
    Sidebar = Color3.fromRGB(8, 9, 12),
    Accent = Color3.fromRGB(140, 80, 255),
    Text = Color3.fromRGB(250, 250, 255),
    DarkText = Color3.fromRGB(150, 155, 170),
    ItemBg = Color3.fromRGB(19, 20, 26)
}

-- مكتبة الألوان مع أشكال المعاينة
local ColorPalette = {
    { Name = "بنفسجي ملكي | Royal Purple", Color = Color3.fromRGB(140, 80, 255) },
    { Name = "أزرق نيون | Neon Blue",       Color = Color3.fromRGB(0, 170, 255) },
    { Name = "أحمر قرمزي | Crimson Red",   Color = Color3.fromRGB(255, 45, 85) },
    { Name = "أخضر زمردي | Emerald Green",Color = Color3.fromRGB(0, 230, 118) },
    { Name = "أصفر ذهبي | Golden Yellow", Color = Color3.fromRGB(255, 193, 7) },
    { Name = "وردي نيون | Neon Pink",       Color = Color3.fromRGB(255, 0, 127) },
    { Name = "سماوي ثلجي | Cyan Ice",       Color = Color3.fromRGB(0, 229, 255) },
    { Name = "برتقالي ناري | Flame Orange", Color = Color3.fromRGB(255, 109, 0) }
}

function MAX_UI:CreateWindow(Options)
    Options = Options or {}
    local TitleText = Options.Name or "MAX HUB"
    local LogoId = Options.Logo or "rbxassetid://6031075931"

    local ParentGui = (gethui and gethui()) or CoreGui or LocalPlayer:WaitForChild("PlayerGui")
    if ParentGui:FindFirstChild("MAX_LuxuryUI_V2") then
        ParentGui.MAX_LuxuryUI_V2:Destroy()
    end

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "MAX_LuxuryUI_V2"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.Parent = ParentGui

    local DefaultSize = IsMobile and UDim2.new(0, 470, 0, 290) or UDim2.new(0, 610, 0, 380)
    local DefaultPos = IsMobile and UDim2.new(0.5, -235, 0.5, -145) or UDim2.new(0.5, -305, 0.5, -190)

    -- 🏢 النافذة الرئيسية
    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = DefaultSize
    MainFrame.Position = DefaultPos
    MainFrame.BackgroundColor3 = Theme.Background
    MainFrame.BorderSizePixel = 0
    MainFrame.Active = true
    MainFrame.Draggable = true
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 12)
    MainCorner.Parent = MainFrame

    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = Theme.Accent
    MainStroke.Thickness = 1.5
    MainStroke.Parent = MainFrame

    -- 🔲 زر الإخفاء المربع العصري بالشعار (Square Reopen Button)
    local ReopenBtn = Instance.new("Frame")
    ReopenBtn.Name = "ReopenSquareButton"
    ReopenBtn.Size = UDim2.new(0, 44, 0, 44)
    ReopenBtn.Position = UDim2.new(0, 20, 0.2, 0)
    ReopenBtn.BackgroundColor3 = Theme.Background
    ReopenBtn.Visible = false
    ReopenBtn.Active = true
    ReopenBtn.Parent = ScreenGui

    local ReopenCorner = Instance.new("UICorner")
    ReopenCorner.CornerRadius = UDim.new(0, 10)
    ReopenCorner.Parent = ReopenBtn

    local ReopenStroke = Instance.new("UIStroke")
    ReopenStroke.Color = Theme.Accent
    ReopenStroke.Thickness = 1.5
    ReopenStroke.Parent = ReopenBtn

    local ReopenLogo = Instance.new("ImageLabel")
    ReopenLogo.Size = UDim2.new(0, 28, 0, 28)
    ReopenLogo.Position = UDim2.new(0.5, -14, 0.5, -14)
    ReopenLogo.BackgroundTransparency = 1
    ReopenLogo.Image = LogoId
    ReopenLogo.Parent = ReopenBtn

    local ClickTrigger = Instance.new("TextButton")
    ClickTrigger.Size = UDim2.new(1, 0, 1, 0)
    ClickTrigger.BackgroundTransparency = 1
    ClickTrigger.Text = ""
    ClickTrigger.Parent = ReopenBtn

    -- إمكانية سحب المربع على الشاشة
    ReopenBtn.Draggable = true

    ClickTrigger.MouseButton1Click:Connect(function()
        MainFrame.Visible = true
        ReopenBtn.Visible = false
    end)

    -- 🔔 نظام الإشعارات المستقل الفخم (Modern Toast Notification)
    function MAX_UI:Notify(Title, Content, Duration)
        Duration = Duration or 3.5
        local NotifyFrame = Instance.new("Frame")
        NotifyFrame.Size = UDim2.new(0, 230, 0, 52)
        NotifyFrame.Position = UDim2.new(1, 10, 1, -70)
        NotifyFrame.BackgroundColor3 = Theme.Background
        NotifyFrame.Parent = ScreenGui

        local NCorner = Instance.new("UICorner")
        NCorner.CornerRadius = UDim.new(0, 10)
        NCorner.Parent = NotifyFrame

        local NStroke = Instance.new("UIStroke")
        NStroke.Color = Theme.Accent
        NStroke.Thickness = 1
        NStroke.Parent = NotifyFrame

        local NIcon = Instance.new("ImageLabel")
        NIcon.Size = UDim2.new(0, 20, 0, 20)
        NIcon.Position = UDim2.new(0, 10, 0, 8)
        NIcon.BackgroundTransparency = 1
        NIcon.Image = LogoId
        NIcon.Parent = NotifyFrame

        local NTitle = Instance.new("TextLabel")
        NTitle.Size = UDim2.new(1, -38, 0, 20)
        NTitle.Position = UDim2.new(0, 34, 0, 6)
        NTitle.BackgroundTransparency = 1
        NTitle.Text = Title
        NTitle.TextColor3 = Theme.Accent
        NTitle.Font = Enum.Font.GothamBold
        NTitle.TextSize = 11
        NTitle.TextXAlignment = Enum.TextXAlignment.Left
        NTitle.Parent = NotifyFrame

        local NText = Instance.new("TextLabel")
        NText.Size = UDim2.new(1, -16, 0, 20)
        NText.Position = UDim2.new(0, 10, 0, 26)
        NText.BackgroundTransparency = 1
        NText.Text = Content
        NText.TextColor3 = Theme.Text
        NText.Font = Enum.Font.GothamMedium
        NText.TextSize = 10
        NText.TextXAlignment = Enum.TextXAlignment.Left
        NText.Parent = NotifyFrame

        TweenService:Create(NotifyFrame, TweenInfo.new(0.35, Enum.EasingStyle.Quart), {Position = UDim2.new(1, -245, 1, -70)}):Play()
        task.delay(Duration, function()
            TweenService:Create(NotifyFrame, TweenInfo.new(0.35, Enum.EasingStyle.Quart), {Position = UDim2.new(1, 10, 1, -70)}):Play()
            task.wait(0.35)
            NotifyFrame:Destroy()
        end)
    end

    -- 📊 الشريط العلوي (Topbar Header)
    local Topbar = Instance.new("Frame")
    Topbar.Size = UDim2.new(1, -16, 0, 32)
    Topbar.Position = UDim2.new(0, 8, 0, 8)
    Topbar.BackgroundColor3 = Theme.Sidebar
    Topbar.Parent = MainFrame

    local TopbarCorner = Instance.new("UICorner")
    TopbarCorner.CornerRadius = UDim.new(0, 8)
    TopbarCorner.Parent = Topbar

    -- الشعار أعلى الزاوية اليسرى من الهيدر
    local HeaderLogo = Instance.new("ImageLabel")
    HeaderLogo.Size = UDim2.new(0, 20, 0, 20)
    HeaderLogo.Position = UDim2.new(0, 8, 0.5, -10)
    HeaderLogo.BackgroundTransparency = 1
    HeaderLogo.Image = LogoId
    HeaderLogo.Parent = Topbar

    local StatsLabel = Instance.new("TextLabel")
    StatsLabel.Size = UDim2.new(1, -110, 1, 0)
    StatsLabel.Position = UDim2.new(0, 34, 0, 0)
    StatsLabel.BackgroundTransparency = 1
    StatsLabel.TextColor3 = Theme.Text
    StatsLabel.Font = Enum.Font.GothamBold
    StatsLabel.TextSize = IsMobile and 10 or 11
    StatsLabel.TextXAlignment = Enum.TextXAlignment.Left
    StatsLabel.Text = TitleText .. " | ⚡ FPS: ... | 📡 Ping: ..."
    StatsLabel.Parent = Topbar

    -- أزرار التحكم العلوي (إغلاق / إخفاء)
    local ControlFrame = Instance.new("Frame")
    ControlFrame.Size = UDim2.new(0, 60, 1, 0)
    ControlFrame.Position = UDim2.new(1, -65, 0, 0)
    ControlFrame.BackgroundTransparency = 1
    ControlFrame.Parent = Topbar

    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.Size = UDim2.new(0, 24, 0, 24)
    MinimizeBtn.Position = UDim2.new(0, 2, 0.5, -12)
    MinimizeBtn.BackgroundTransparency = 1
    MinimizeBtn.Text = "-"
    MinimizeBtn.TextColor3 = Theme.Text
    MinimizeBtn.Font = Enum.Font.GothamBold
    MinimizeBtn.TextSize = 18
    MinimizeBtn.Parent = ControlFrame

    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 24, 0, 24)
    CloseBtn.Position = UDim2.new(0, 30, 0.5, -12)
    CloseBtn.BackgroundTransparency = 1
    CloseBtn.Text = "✕"
    CloseBtn.TextColor3 = Color3.fromRGB(255, 70, 70)
    CloseBtn.Font = Enum.Font.GothamBold
    CloseBtn.TextSize = 13
    CloseBtn.Parent = ControlFrame

    MinimizeBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = false
        ReopenBtn.Visible = true
        MAX_UI:Notify("👑 " .. TitleText, "تم تصغير الواجهة، اضغط المربع للفتح", 2)
    end)

    CloseBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
    end)

    -- حساب الفريمات اللحظية
    local frameCount = 0
    local lastTime = tick()
    RunService.RenderStepped:Connect(function()
        frameCount = frameCount + 1
        local currentTime = tick()
        if currentTime - lastTime >= 0.25 then
            local realFPS = math.floor(frameCount / (currentTime - lastTime))
            local pingVal = 0
            pcall(function()
                pingVal = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
            end)
            StatsLabel.Text = string.format("%s | ⚡ FPS: %d | 📡 Ping: %dms", TitleText, realFPS, pingVal)
            frameCount = 0
            lastTime = currentTime
        end
    end)

    -- 📁 الشريط الجانبي (Sidebar)
    local SidebarWidth = IsMobile and 135 or 165
    local Sidebar = Instance.new("Frame")
    Sidebar.Size = UDim2.new(0, SidebarWidth, 1, -52)
    Sidebar.Position = UDim2.new(0, 8, 0, 44)
    Sidebar.BackgroundColor3 = Theme.Sidebar
    Sidebar.Parent = MainFrame

    local SidebarCorner = Instance.new("UICorner")
    SidebarCorner.CornerRadius = UDim.new(0, 10)
    SidebarCorner.Parent = Sidebar

    -- 👤 بروفايل المستخدم الفاخر
    local UserFrame = Instance.new("Frame")
    UserFrame.Size = UDim2.new(1, -12, 0, 42)
    UserFrame.Position = UDim2.new(0, 6, 1, -48)
    UserFrame.BackgroundColor3 = Theme.ItemBg
    UserFrame.Parent = Sidebar

    local UserCorner = Instance.new("UICorner")
    UserCorner.CornerRadius = UDim.new(0, 8)
    UserCorner.Parent = UserFrame

    local UserAvatar = Instance.new("ImageLabel")
    UserAvatar.Size = UDim2.new(0, 28, 0, 28)
    UserAvatar.Position = UDim2.new(0, 6, 0.5, -14)
    UserAvatar.BackgroundTransparency = 1
    pcall(function()
        UserAvatar.Image = Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
    end)
    UserAvatar.Parent = UserFrame

    local AvatarCorner = Instance.new("UICorner")
    AvatarCorner.CornerRadius = UDim.new(1, 0)
    AvatarCorner.Parent = UserAvatar

    local UserDisplayName = Instance.new("TextLabel")
    UserDisplayName.Size = UDim2.new(1, -40, 0, 16)
    UserDisplayName.Position = UDim2.new(0, 38, 0, 5)
    UserDisplayName.BackgroundTransparency = 1
    UserDisplayName.Text = LocalPlayer.DisplayName
    UserDisplayName.TextColor3 = Theme.Text
    UserDisplayName.Font = Enum.Font.GothamBold
    UserDisplayName.TextSize = 10
    UserDisplayName.TextXAlignment = Enum.TextXAlignment.Left
    UserDisplayName.Parent = UserFrame

    local UsernameLabel = Instance.new("TextLabel")
    UsernameLabel.Size = UDim2.new(1, -40, 0, 14)
    UsernameLabel.Position = UDim2.new(0, 38, 0, 21)
    UsernameLabel.BackgroundTransparency = 1
    UsernameLabel.Text = "@" .. LocalPlayer.Name
    UsernameLabel.TextColor3 = Theme.DarkText
    UsernameLabel.Font = Enum.Font.GothamMedium
    UsernameLabel.TextSize = 9
    UsernameLabel.TextXAlignment = Enum.TextXAlignment.Left
    UsernameLabel.Parent = UserFrame

    local TabButtonContainer = Instance.new("ScrollingFrame")
    TabButtonContainer.Size = UDim2.new(1, -12, 1, -58)
    TabButtonContainer.Position = UDim2.new(0, 6, 0, 8)
    TabButtonContainer.BackgroundTransparency = 1
    TabButtonContainer.ScrollBarThickness = 0
    TabButtonContainer.Parent = Sidebar

    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 5)
    TabListLayout.Parent = TabButtonContainer

    local ContentArea = Instance.new("Frame")
    ContentArea.Size = UDim2.new(1, -(SidebarWidth + 24), 1, -52)
    ContentArea.Position = UDim2.new(0, SidebarWidth + 16, 0, 44)
    ContentArea.BackgroundTransparency = 1
    ContentArea.Parent = MainFrame

    local WindowObj = { Tabs = {}, Accents = {} }

    function WindowObj:CreateTab(TabName, IconId)
        IconId = IconId or "rbxassetid://6031075929"
        local TabButton = Instance.new("TextButton")
        TabButton.Size = UDim2.new(1, 0, 0, 30)
        TabButton.BackgroundColor3 = Theme.ItemBg
        TabButton.Text = ""
        TabButton.AutoButtonColor = false
        TabButton.Parent = TabButtonContainer

        local BtnCorner = Instance.new("UICorner")
        BtnCorner.CornerRadius = UDim.new(0, 6)
        BtnCorner.Parent = TabButton

        local TabIcon = Instance.new("ImageLabel")
        TabIcon.Size = UDim2.new(0, 15, 0, 15)
        TabIcon.Position = UDim2.new(0, 8, 0.5, -7)
        TabIcon.BackgroundTransparency = 1
        TabIcon.Image = IconId
        TabIcon.ImageColor3 = Theme.DarkText
        TabIcon.Parent = TabButton

        local TabTitle = Instance.new("TextLabel")
        TabTitle.Size = UDim2.new(1, -28, 1, 0)
        TabTitle.Position = UDim2.new(0, 26, 0, 0)
        TabTitle.BackgroundTransparency = 1
        TabTitle.Text = TabName
        TabTitle.TextColor3 = Theme.DarkText
        TabTitle.Font = Enum.Font.GothamMedium
        TabTitle.TextSize = IsMobile and 10 or 11
        TabTitle.TextXAlignment = Enum.TextXAlignment.Left
        TabTitle.Parent = TabButton

        local PageFrame = Instance.new("ScrollingFrame")
        PageFrame.Size = UDim2.new(1, 0, 1, 0)
        PageFrame.BackgroundTransparency = 1
        PageFrame.Visible = false
        PageFrame.ScrollBarThickness = 2
        PageFrame.ScrollBarImageColor3 = Theme.Accent
        PageFrame.Parent = ContentArea

        local PageListLayout = Instance.new("UIListLayout")
        PageListLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageListLayout.Padding = UDim.new(0, 6)
        PageListLayout.Parent = PageFrame

        TabButton.MouseButton1Click:Connect(function()
            for _, tab in ipairs(WindowObj.Tabs) do
                tab.Page.Visible = false
                tab.Icon.ImageColor3 = Theme.DarkText
                tab.Title.TextColor3 = Theme.DarkText
                tab.Button.BackgroundColor3 = Theme.ItemBg
            end
            PageFrame.Visible = true
            TabIcon.ImageColor3 = Theme.Text
            TabTitle.TextColor3 = Theme.Text
            TabButton.BackgroundColor3 = Theme.Accent
        end)

        local TabElements = { Page = PageFrame, Button = TabButton, Icon = TabIcon, Title = TabTitle }

        function TabElements:AddButton(Text, Callback)
            Callback = Callback or function() end
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(1, -6, 0, 32)
            Btn.BackgroundColor3 = Theme.ItemBg
            Btn.Text = Text
            Btn.TextColor3 = Theme.Text
            Btn.Font = Enum.Font.GothamBold
            Btn.TextSize = 11
            Btn.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Btn

            Btn.MouseButton1Click:Connect(function()
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.Accent}):Play()
                task.wait(0.1)
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.ItemBg}):Play()
                Callback()
            end)
        end

        function TabElements:AddToggle(Text, Default, Callback)
            Callback = Callback or function() end
            local Toggled = Default or false

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 32)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(1, -40, 1, 0)
            Label.Position = UDim2.new(0, 10, 0, 0)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.Text
            Label.Font = Enum.Font.GothamMedium
            Label.TextSize = 11
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame

            local Indicator = Instance.new("TextButton")
            Indicator.Size = UDim2.new(0, 18, 0, 18)
            Indicator.Position = UDim2.new(1, -26, 0.5, -9)
            Indicator.BackgroundColor3 = Toggled and Theme.Accent or Color3.fromRGB(40, 40, 50)
            Indicator.Text = ""
            Indicator.Parent = Frame

            table.insert(WindowObj.Accents, function(col)
                if Toggled then Indicator.BackgroundColor3 = col end
            end)

            local IndCorner = Instance.new("UICorner")
            IndCorner.CornerRadius = UDim.new(0, 4)
            IndCorner.Parent = Indicator

            local function UpdateToggle()
                Toggled = not Toggled
                Indicator.BackgroundColor3 = Toggled and Theme.Accent or Color3.fromRGB(40, 40, 50)
                Callback(Toggled)
            end

            Indicator.MouseButton1Click:Connect(UpdateToggle)
        end

        function TabElements:AddSlider(Text, Min, Max, Default, Callback)
            Callback = Callback or function() end
            local Value = Default or Min

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 42)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(1, -50, 0, 18)
            Label.Position = UDim2.new(0, 10, 0, 4)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.Text
            Label.Font = Enum.Font.GothamMedium
            Label.TextSize = 11
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame

            local ValLabel = Instance.new("TextLabel")
            ValLabel.Size = UDim2.new(0, 40, 0, 18)
            ValLabel.Position = UDim2.new(1, -45, 0, 4)
            ValLabel.BackgroundTransparency = 1
            ValLabel.Text = tostring(Value)
            ValLabel.TextColor3 = Theme.Accent
            ValLabel.Font = Enum.Font.GothamBold
            ValLabel.TextSize = 11
            ValLabel.Parent = Frame

            table.insert(WindowObj.Accents, function(col) ValLabel.TextColor3 = col end)

            local SliderBar = Instance.new("Frame")
            SliderBar.Size = UDim2.new(1, -20, 0, 5)
            SliderBar.Position = UDim2.new(0, 10, 1, -10)
            SliderBar.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
            SliderBar.Parent = Frame

            local SliderFill = Instance.new("Frame")
            SliderFill.Size = UDim2.new((Value - Min)/(Max - Min), 0, 1, 0)
            SliderFill.BackgroundColor3 = Theme.Accent
            SliderFill.BorderSizePixel = 0
            SliderFill.Parent = SliderBar

            table.insert(WindowObj.Accents, function(col) SliderFill.BackgroundColor3 = col end)

            local Dragging = false
            local lastUpdate = 0

            local function Update(input)
                local now = tick()
                if now - lastUpdate < 0.02 then return end
                lastUpdate = now

                local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
                Value = math.floor(Min + (Max - Min) * pos)
                ValLabel.Text = tostring(Value)
                SliderFill.Size = UDim2.new(pos, 0, 1, 0)
                Callback(Value)
            end

            SliderBar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = true; Update(input)
                end
            end)

            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = false
                end
            end)

            UserInputService.InputChanged:Connect(function(input)
                if Dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    Update(input)
                end
            end)
        end

        -- 🎨 قائمة الألوان المنسدلة مع مربع المعاينة (Dropdown Palette)
        function TabElements:AddColorDropdown(Text, Callback)
            Callback = Callback or function() end
            local Expanded = false

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 32)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.ClipsDescendants = true
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local DropBtn = Instance.new("TextButton")
            DropBtn.Size = UDim2.new(1, 0, 0, 32)
            DropBtn.BackgroundTransparency = 1
            DropBtn.Text = "  " .. Text
            DropBtn.TextColor3 = Theme.Text
            DropBtn.Font = Enum.Font.GothamMedium
            DropBtn.TextSize = 11
            DropBtn.TextXAlignment = Enum.TextXAlignment.Left
            DropBtn.Parent = Frame

            local Arrow = Instance.new("TextLabel")
            Arrow.Size = UDim2.new(0, 20, 0, 32)
            Arrow.Position = UDim2.new(1, -25, 0, 0)
            Arrow.BackgroundTransparency = 1
            Arrow.Text = "▼"
            Arrow.TextColor3 = Theme.DarkText
            Arrow.Font = Enum.Font.GothamBold
            Arrow.TextSize = 10
            Arrow.Parent = Frame

            local Container = Instance.new("Frame")
            Container.Size = UDim2.new(1, -12, 0, #ColorPalette * 28)
            Container.Position = UDim2.new(0, 6, 0, 34)
            Container.BackgroundTransparency = 1
            Container.Parent = Frame

            local CLayout = Instance.new("UIListLayout")
            CLayout.Padding = UDim.new(0, 3)
            CLayout.Parent = Container

            for _, item in ipairs(ColorPalette) do
                local ItemBtn = Instance.new("TextButton")
                ItemBtn.Size = UDim2.new(1, 0, 0, 25)
                ItemBtn.BackgroundColor3 = Color3.fromRGB(26, 28, 36)
                ItemBtn.Text = "      " .. item.Name
                ItemBtn.TextColor3 = Theme.Text
                ItemBtn.Font = Enum.Font.GothamMedium
                ItemBtn.TextSize = 10
                ItemBtn.TextXAlignment = Enum.TextXAlignment.Left
                ItemBtn.Parent = Container

                local ICorner = Instance.new("UICorner")
                ICorner.CornerRadius = UDim.new(0, 4)
                ICorner.Parent = ItemBtn

                -- مربع معاينة شكل اللون بجانب الاسم
                local ColorBox = Instance.new("Frame")
                ColorBox.Size = UDim2.new(0, 14, 0, 14)
                ColorBox.Position = UDim2.new(0, 6, 0.5, -7)
                ColorBox.BackgroundColor3 = item.Color
                ColorBox.Parent = ItemBtn

                local BoxCorner = Instance.new("UICorner")
                BoxCorner.CornerRadius = UDim.new(0, 3)
                BoxCorner.Parent = ColorBox

                ItemBtn.MouseButton1Click:Connect(function()
                    Expanded = false
                    TweenService:Create(Frame, TweenInfo.new(0.2), {Size = UDim2.new(1, -6, 0, 32)}):Play()
                    Arrow.Text = "▼"
                    Callback(item.Color)
                end)
            end

            DropBtn.MouseButton1Click:Connect(function()
                Expanded = not Expanded
                local targetHeight = Expanded and (38 + #ColorPalette * 28) or 32
                TweenService:Create(Frame, TweenInfo.new(0.2), {Size = UDim2.new(1, -6, 0, targetHeight)}):Play()
                Arrow.Text = Expanded and "▲" or "▼"
            end)
        end

        table.insert(WindowObj.Tabs, TabElements)

        if #WindowObj.Tabs == 1 then
            PageFrame.Visible = true
            TabIcon.ImageColor3 = Theme.Text
            TabTitle.TextColor3 = Theme.Text
            TabButton.BackgroundColor3 = Theme.Accent
        end

        return TabElements
    end

    -- ============================================================
    -- 🎨 تبويب الإعدادات مع منتقي الألوان المعاين
    -- ============================================================
    local SettingsTab = WindowObj:CreateTab("الإعدادات ⚙️")

    local function ApplyColor(col)
        Theme.Accent = col
        MainStroke.Color = col
        ReopenStroke.Color = col
        for _, updateFunc in ipairs(WindowObj.Accents) do updateFunc(col) end
        MAX_UI:Notify("👑 MAX HUB", "تم تحديث ثيم الواجهة!", 2)
    end

    SettingsTab:AddColorDropdown("اختر لون الثيم الرئيسي", function(selectedColor)
        ApplyColor(selectedColor)
    end)

    -- 🌟 إرسال رسالة الترحيب الأنيقة عند تشغيل الواجهة
    MAX_UI:Notify("مرحباً بك 👑", "أهلاً بك يا " .. LocalPlayer.DisplayName .. " في MAX HUB", 4)

    return WindowObj
end

return MAX_UI
