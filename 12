-- ============================================================
-- 👑 MAX TITAN ULTRA LUXURY UI LIBRARY (Engine V20 - Royal Edition)
-- 🌐 Full Standalone Production Code | Pure Royal Purple Theme
-- ============================================================

local MAX_UI = {}

-- ⚙️ الخدمات الأساسية لنظام روبلوكس
local UserInputService = game:GetService("UserInputService")
local TweenService    = game:GetService("TweenService")
local RunService      = game:GetService("RunService")
local CoreGui         = game:GetService("CoreGui")
local Players         = game:GetService("Players")
local Stats           = game:GetService("Stats")
local SoundService    = game:GetService("SoundService")
local Lighting        = game:GetService("Lighting")

local LocalPlayer = Players.LocalPlayer
local IsMobile    = UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled

-- 🔓 فتح سقف الفريمات فور تشغيل السكربت
pcall(function()
    if setfpscap then setfpscap(240)
    elseif set_fps_cap then set_fps_cap(240) end
end)

-- 🎨 ثيم البنفسجي الملكي الموحد والأسود الفاخر
local Theme = {
    Background  = Color3.fromRGB(10, 11, 15),
    Sidebar     = Color3.fromRGB(7, 8, 11),
    Header      = Color3.fromRGB(14, 15, 20),
    Accent      = Color3.fromRGB(138, 43, 226), -- بنفسجي ملكي مشع
    AccentDark  = Color3.fromRGB(90, 25, 150),
    TextPrimary = Color3.fromRGB(255, 255, 255),
    TextDark    = Color3.fromRGB(170, 175, 190),
    ItemBg      = Color3.fromRGB(16, 17, 24),
    ItemHover   = Color3.fromRGB(24, 25, 36),
    Border      = Color3.fromRGB(138, 43, 226),
    Disabled    = Color3.fromRGB(35, 38, 48)
}

-- 🖼️ شعار المطور الخاص بك
local DeveloperLogo = "rbxassetid://96007556931310"

-- 🔊 دالة تشغيل صوت الإشعار الترحيبي والتنبيهي
local function PlayNotifySound()
    pcall(function()
        local sound = Instance.new("Sound")
        sound.SoundId = "rbxassetid://4590662766"
        sound.Volume = 0.65
        sound.Parent = SoundService
        sound:Play()
        task.delay(2, function() sound:Destroy() end)
    end)
end

-- ⏰ دالة الترحيب الذكية حسب وقت اليوم
local function GetSmartGreeting()
    local hour = tonumber(os.date("%H")) or 12
    if hour >= 5 and hour < 12 then
        return "صباح الخير ☀️"
    elseif hour >= 12 and hour < 18 then
        return "مساء الخير 🌤️"
    else
        return "أهلاً بك 🌙"
    end
end

-- ============================================================
-- 🛠️ النافذة الرئيسية للواجهة
-- ============================================================
function MAX_UI:CreateWindow(Options)
    Options = Options or {}
    local TitleText = Options.Name or "MAX HUB | TITAN"

    local ParentGui = (gethui and gethui()) or CoreGui or LocalPlayer:WaitForChild("PlayerGui")
    if ParentGui:FindFirstChild("MAX_TitanEngine_V20") then
        ParentGui.MAX_TitanEngine_V20:Destroy()
    end

    local ScreenGui = Instance.new("ScreenGui")
    ScreenGui.Name = "MAX_TitanEngine_V20"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    ScreenGui.Parent = ParentGui

    local DefaultSize = IsMobile and UDim2.new(0, 520, 0, 330) or UDim2.new(0, 660, 0, 430)
    local DefaultPos  = IsMobile and UDim2.new(0.5, -260, 0.5, -165) or UDim2.new(0.5, -330, 0.5, -215)

    local MainFrame = Instance.new("Frame")
    MainFrame.Name = "MainFrame"
    MainFrame.Size = DefaultSize
    MainFrame.Position = DefaultPos
    MainFrame.BackgroundColor3 = Theme.Background
    MainFrame.BorderSizePixel = 0
    MainFrame.Active = true
    MainFrame.Draggable = true
    MainFrame.ClipsDescendants = true
    MainFrame.Parent = ScreenGui

    local MainCorner = Instance.new("UICorner")
    MainCorner.CornerRadius = UDim.new(0, 12)
    MainCorner.Parent = MainFrame

    local MainStroke = Instance.new("UIStroke")
    MainStroke.Color = Theme.Accent
    MainStroke.Thickness = 1.8
    MainStroke.Parent = MainFrame

    -- 🔲 زر الإعادة / التصغير العائم بشعار المطور
    local ReopenBtn = Instance.new("Frame")
    ReopenBtn.Name = "ReopenSquare"
    ReopenBtn.Size = UDim2.new(0, 48, 0, 48)
    ReopenBtn.Position = UDim2.new(0, 20, 0.2, 0)
    ReopenBtn.BackgroundColor3 = Theme.Background
    ReopenBtn.Visible = false
    ReopenBtn.Active = true
    ReopenBtn.Draggable = true
    ReopenBtn.Parent = ScreenGui

    local ReopenCorner = Instance.new("UICorner")
    ReopenCorner.CornerRadius = UDim.new(0, 10)
    ReopenCorner.Parent = ReopenBtn

    local ReopenStroke = Instance.new("UIStroke")
    ReopenStroke.Color = Theme.Accent
    ReopenStroke.Thickness = 1.8
    ReopenStroke.Parent = ReopenBtn

    local ReopenLogo = Instance.new("ImageLabel")
    ReopenLogo.Size = UDim2.new(0, 32, 0, 32)
    ReopenLogo.Position = UDim2.new(0.5, -16, 0.5, -16)
    ReopenLogo.BackgroundTransparency = 1
    ReopenLogo.Image = DeveloperLogo
    ReopenLogo.Parent = ReopenBtn

    local ReopenLogoCorner = Instance.new("UICorner")
    ReopenLogoCorner.CornerRadius = UDim.new(1, 0)
    ReopenLogoCorner.Parent = ReopenLogo

    local ReopenTrigger = Instance.new("TextButton")
    ReopenTrigger.Size = UDim2.new(1, 0, 1, 0)
    ReopenTrigger.BackgroundTransparency = 1
    ReopenTrigger.Text = ""
    ReopenTrigger.Parent = ReopenBtn

    ReopenTrigger.MouseButton1Click:Connect(function()
        MainFrame.Visible = true
        ReopenBtn.Visible = false
    end)

    -- 🔔 نظام الإشعارات الملكي السفلي المصحوب بالصوت
    local function NotifySystem(Title, Content, Duration)
        Duration = Duration or 3
        PlayNotifySound()

        local NotifyFrame = Instance.new("Frame")
        NotifyFrame.Size = UDim2.new(0, 260, 0, 56)
        NotifyFrame.Position = UDim2.new(1, 20, 1, -85)
        NotifyFrame.BackgroundColor3 = Theme.Header
        NotifyFrame.ClipsDescendants = true
        NotifyFrame.Parent = ScreenGui

        local NCorner = Instance.new("UICorner")
        NCorner.CornerRadius = UDim.new(0, 10)
        NCorner.Parent = NotifyFrame

        local NStroke = Instance.new("UIStroke")
        NStroke.Color = Theme.Accent
        NStroke.Thickness = 1.4
        NStroke.Parent = NotifyFrame

        local NIcon = Instance.new("ImageLabel")
        NIcon.Size = UDim2.new(0, 28, 0, 28)
        NIcon.Position = UDim2.new(0, 10, 0, 14)
        NIcon.BackgroundTransparency = 1
        NIcon.Image = DeveloperLogo
        NIcon.Parent = NotifyFrame

        local NIconCorner = Instance.new("UICorner")
        NIconCorner.CornerRadius = UDim.new(1, 0)
        NIconCorner.Parent = NIcon

        local NTitle = Instance.new("TextLabel")
        NTitle.Size = UDim2.new(1, -50, 0, 18)
        NTitle.Position = UDim2.new(0, 44, 0, 8)
        NTitle.BackgroundTransparency = 1
        NTitle.Text = Title
        NTitle.TextColor3 = Theme.Accent
        NTitle.Font = Enum.Font.GothamBold
        NTitle.TextSize = 12
        NTitle.TextXAlignment = Enum.TextXAlignment.Left
        NTitle.Parent = NotifyFrame

        local NText = Instance.new("TextLabel")
        NText.Size = UDim2.new(1, -50, 0, 18)
        NText.Position = UDim2.new(0, 44, 0, 28)
        NText.BackgroundTransparency = 1
        NText.Text = Content
        NText.TextColor3 = Theme.TextPrimary
        NText.Font = Enum.Font.GothamBold
        NText.TextSize = 10
        NText.TextXAlignment = Enum.TextXAlignment.Left
        NText.Parent = NotifyFrame

        local TimeBar = Instance.new("Frame")
        TimeBar.Size = UDim2.new(1, 0, 0, 3)
        TimeBar.Position = UDim2.new(0, 0, 1, -3)
        TimeBar.BackgroundColor3 = Theme.Accent
        TimeBar.BorderSizePixel = 0
        TimeBar.Parent = NotifyFrame

        TweenService:Create(NotifyFrame, TweenInfo.new(0.35, Enum.EasingStyle.Quart), {Position = UDim2.new(1, -275, 1, -85)}):Play()
        TweenService:Create(TimeBar, TweenInfo.new(Duration, Enum.EasingStyle.Linear), {Size = UDim2.new(0, 0, 0, 3)}):Play()

        task.delay(Duration, function()
            TweenService:Create(NotifyFrame, TweenInfo.new(0.35, Enum.EasingStyle.Quart), {Position = UDim2.new(1, 20, 1, -85)}):Play()
            task.wait(0.35)
            NotifyFrame:Destroy()
        end)
    end

    MAX_UI.Notify = NotifySystem

    -- 📊 الهيدر العلوي
    local Topbar = Instance.new("Frame")
    Topbar.Size = UDim2.new(1, -16, 0, 36)
    Topbar.Position = UDim2.new(0, 8, 0, 8)
    Topbar.BackgroundColor3 = Theme.Header
    Topbar.Parent = MainFrame

    local TopbarCorner = Instance.new("UICorner")
    TopbarCorner.CornerRadius = UDim.new(0, 8)
    TopbarCorner.Parent = Topbar

    local HeaderLogo = Instance.new("ImageLabel")
    HeaderLogo.Size = UDim2.new(0, 24, 0, 24)
    HeaderLogo.Position = UDim2.new(0, 8, 0.5, -12)
    HeaderLogo.BackgroundTransparency = 1
    HeaderLogo.Image = DeveloperLogo
    HeaderLogo.Parent = Topbar

    local HeaderCorner = Instance.new("UICorner")
    HeaderCorner.CornerRadius = UDim.new(1, 0)
    HeaderCorner.Parent = HeaderLogo

    local StatsLabel = Instance.new("TextLabel")
    StatsLabel.Size = UDim2.new(1, -120, 1, 0)
    StatsLabel.Position = UDim2.new(0, 38, 0, 0)
    StatsLabel.BackgroundTransparency = 1
    StatsLabel.TextColor3 = Theme.TextPrimary
    StatsLabel.Font = Enum.Font.GothamBold
    StatsLabel.TextSize = IsMobile and 10 or 11
    StatsLabel.TextXAlignment = Enum.TextXAlignment.Left
    StatsLabel.Text = TitleText .. "  |  ⚡ FPS: ...  |  📡 Ping: ..."
    StatsLabel.Parent = Topbar

    -- 🔘 أزرار التحكم بالنافذة
    local ControlFrame = Instance.new("Frame")
    ControlFrame.Size = UDim2.new(0, 68, 1, 0)
    ControlFrame.Position = UDim2.new(1, -72, 0, 0)
    ControlFrame.BackgroundTransparency = 1
    ControlFrame.Parent = Topbar

    local MinimizeBtn = Instance.new("TextButton")
    MinimizeBtn.Size = UDim2.new(0, 26, 0, 22)
    MinimizeBtn.Position = UDim2.new(0, 4, 0.5, -11)
    MinimizeBtn.BackgroundColor3 = Color3.fromRGB(24, 26, 36)
    MinimizeBtn.Text = ""
    MinimizeBtn.AutoButtonColor = false
    MinimizeBtn.Parent = ControlFrame

    local MinCorner = Instance.new("UICorner")
    MinCorner.CornerRadius = UDim.new(0, 6)
    MinCorner.Parent = MinimizeBtn

    local MinLine = Instance.new("Frame")
    MinLine.Size = UDim2.new(0, 10, 0, 2)
    MinLine.Position = UDim2.new(0.5, -5, 0.5, -1)
    MinLine.BackgroundColor3 = Theme.TextPrimary
    MinLine.BorderSizePixel = 0
    MinLine.Parent = MinimizeBtn

    local CloseBtn = Instance.new("TextButton")
    CloseBtn.Size = UDim2.new(0, 26, 0, 22)
    CloseBtn.Position = UDim2.new(0, 36, 0.5, -11)
    CloseBtn.BackgroundColor3 = Color3.fromRGB(45, 20, 25)
    CloseBtn.Text = ""
    CloseBtn.AutoButtonColor = false
    CloseBtn.Parent = ControlFrame

    local CloseCorner = Instance.new("UICorner")
    CloseCorner.CornerRadius = UDim.new(0, 6)
    CloseCorner.Parent = CloseBtn

    local CrossIcon = Instance.new("ImageLabel")
    CrossIcon.Size = UDim2.new(0, 12, 0, 12)
    CrossIcon.Position = UDim2.new(0.5, -6, 0.5, -6)
    CrossIcon.BackgroundTransparency = 1
    CrossIcon.Image = "rbxassetid://5054666997"
    CrossIcon.ImageColor3 = Color3.fromRGB(255, 80, 80)
    CrossIcon.Parent = CloseBtn

    MinimizeBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = false
        ReopenBtn.Visible = true
    end)

    CloseBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
    end)

    -- حساب الفريمات واستجابة السيرفر
    local frameCount = 0
    local lastTime = tick()
    RunService.RenderStepped:Connect(function()
        frameCount = frameCount + 1
        local currentTime = tick()
        if currentTime - lastTime >= 0.25 then
            local realFPS = math.floor(frameCount / (currentTime - lastTime))
            local pingVal = 0
            pcall(function()
                pingVal = math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
            end)
            StatsLabel.Text = string.format("%s  |  ⚡ FPS: %d  |  📡 Ping: %dms", TitleText, realFPS, pingVal)
            frameCount = 0
            lastTime = currentTime
        end
    end)

    -- 📁 الشريط الجانبي للتبويبات
    local SidebarWidth = IsMobile and 150 or 180
    local Sidebar = Instance.new("Frame")
    Sidebar.Size = UDim2.new(0, SidebarWidth, 1, -56)
    Sidebar.Position = UDim2.new(0, 8, 0, 48)
    Sidebar.BackgroundColor3 = Theme.Sidebar
    Sidebar.Parent = MainFrame

    local SidebarCorner = Instance.new("UICorner")
    SidebarCorner.CornerRadius = UDim.new(0, 10)
    SidebarCorner.Parent = Sidebar

    -- 👑 بطاقة المطور والبروفايل
    local WelcomeCard = Instance.new("Frame")
    WelcomeCard.Size = UDim2.new(1, -12, 0, 52)
    WelcomeCard.Position = UDim2.new(0, 6, 0, 6)
    WelcomeCard.BackgroundColor3 = Theme.ItemBg
    WelcomeCard.Parent = Sidebar

    local CardCorner = Instance.new("UICorner")
    CardCorner.CornerRadius = UDim.new(0, 8)
    CardCorner.Parent = WelcomeCard

    local CardStroke = Instance.new("UIStroke")
    CardStroke.Color = Theme.Accent
    CardStroke.Thickness = 1.2
    CardStroke.Parent = WelcomeCard

    local UserAvatar = Instance.new("ImageLabel")
    UserAvatar.Size = UDim2.new(0, 36, 0, 36)
    UserAvatar.Position = UDim2.new(0, 6, 0.5, -18)
    UserAvatar.BackgroundTransparency = 1
    UserAvatar.Image = DeveloperLogo
    UserAvatar.Parent = WelcomeCard

    local AvatarCorner = Instance.new("UICorner")
    AvatarCorner.CornerRadius = UDim.new(1, 0)
    AvatarCorner.Parent = UserAvatar

    local GreetingLabel = Instance.new("TextLabel")
    GreetingLabel.Size = UDim2.new(1, -48, 0, 14)
    GreetingLabel.Position = UDim2.new(0, 46, 0, 6)
    GreetingLabel.BackgroundTransparency = 1
    GreetingLabel.Text = GetSmartGreeting()
    GreetingLabel.TextColor3 = Theme.Accent
    GreetingLabel.Font = Enum.Font.GothamBold
    GreetingLabel.TextSize = 9
    GreetingLabel.TextXAlignment = Enum.TextXAlignment.Left
    GreetingLabel.Parent = WelcomeCard

    local UserDisplayName = Instance.new("TextLabel")
    UserDisplayName.Size = UDim2.new(1, -48, 0, 14)
    UserDisplayName.Position = UDim2.new(0, 46, 0, 19)
    UserDisplayName.BackgroundTransparency = 1
    UserDisplayName.Text = LocalPlayer.DisplayName
    UserDisplayName.TextColor3 = Theme.TextPrimary
    UserDisplayName.Font = Enum.Font.GothamBold
    UserDisplayName.TextSize = 10
    UserDisplayName.TextXAlignment = Enum.TextXAlignment.Left
    UserDisplayName.Parent = WelcomeCard

    local BadgeLabel = Instance.new("TextLabel")
    BadgeLabel.Size = UDim2.new(1, -48, 0, 12)
    BadgeLabel.Position = UDim2.new(0, 46, 0, 33)
    BadgeLabel.BackgroundTransparency = 1
    BadgeLabel.Text = "[VIP MEMBER]"
    BadgeLabel.TextColor3 = Theme.Accent
    BadgeLabel.Font = Enum.Font.GothamBold
    BadgeLabel.TextSize = 8
    BadgeLabel.TextXAlignment = Enum.TextXAlignment.Left
    BadgeLabel.Parent = WelcomeCard

    local TabButtonContainer = Instance.new("ScrollingFrame")
    TabButtonContainer.Size = UDim2.new(1, -12, 1, -70)
    TabButtonContainer.Position = UDim2.new(0, 6, 0, 64)
    TabButtonContainer.BackgroundTransparency = 1
    TabButtonContainer.ScrollBarThickness = 0
    TabButtonContainer.Parent = Sidebar

    local TabListLayout = Instance.new("UIListLayout")
    TabListLayout.SortOrder = Enum.SortOrder.LayoutOrder
    TabListLayout.Padding = UDim.new(0, 5)
    TabListLayout.Parent = TabButtonContainer

    local ContentArea = Instance.new("Frame")
    ContentArea.Size = UDim2.new(1, -(SidebarWidth + 24), 1, -56)
    ContentArea.Position = UDim2.new(0, SidebarWidth + 16, 0, 48)
    ContentArea.BackgroundTransparency = 1
    ContentArea.Parent = MainFrame

    local WindowObj = { Tabs = {}, Notify = NotifySystem }

    -- 🌟 دالة إنشاء التبويبات مع اختيار الشعار والآيدي الخاص بك
    function WindowObj:CreateTab(TabName, CustomIcon)
        local IconId = CustomIcon or DeveloperLogo

        local TabButton = Instance.new("TextButton")
        TabButton.Size = UDim2.new(1, 0, 0, 34)
        TabButton.BackgroundColor3 = Theme.ItemBg
        TabButton.Text = ""
        TabButton.AutoButtonColor = false
        TabButton.Parent = TabButtonContainer

        local BtnCorner = Instance.new("UICorner")
        BtnCorner.CornerRadius = UDim.new(0, 6)
        BtnCorner.Parent = TabButton

        local TabIcon = Instance.new("ImageLabel")
        TabIcon.Size = UDim2.new(0, 18, 0, 18)
        TabIcon.Position = UDim2.new(0, 8, 0.5, -9)
        TabIcon.BackgroundTransparency = 1
        TabIcon.Image = IconId
        TabIcon.Parent = TabButton

        local TabIconCorner = Instance.new("UICorner")
        TabIconCorner.CornerRadius = UDim.new(1, 0)
        TabIconCorner.Parent = TabIcon

        local TabTitle = Instance.new("TextLabel")
        TabTitle.Size = UDim2.new(1, -32, 1, 0)
        TabTitle.Position = UDim2.new(0, 30, 0, 0)
        TabTitle.BackgroundTransparency = 1
        TabTitle.Text = TabName
        TabTitle.TextColor3 = Theme.TextDark
        TabTitle.Font = Enum.Font.GothamBold
        TabTitle.TextSize = IsMobile and 10 or 11
        TabTitle.TextXAlignment = Enum.TextXAlignment.Left
        TabTitle.Parent = TabButton

        local PageFrame = Instance.new("ScrollingFrame")
        PageFrame.Size = UDim2.new(1, 0, 1, 0)
        PageFrame.BackgroundTransparency = 1
        PageFrame.Visible = false
        PageFrame.ScrollBarThickness = 2
        PageFrame.ScrollBarImageColor3 = Theme.Accent
        PageFrame.Parent = ContentArea

        local PageListLayout = Instance.new("UIListLayout")
        PageListLayout.SortOrder = Enum.SortOrder.LayoutOrder
        PageListLayout.Padding = UDim.new(0, 8)
        PageListLayout.Parent = PageFrame

        TabButton.MouseButton1Click:Connect(function()
            for _, tab in ipairs(WindowObj.Tabs) do
                tab.Page.Visible = false
                tab.Title.TextColor3 = Theme.TextDark
                tab.Button.BackgroundColor3 = Theme.ItemBg
            end
            PageFrame.Visible = true
            TabTitle.TextColor3 = Theme.TextPrimary
            TabButton.BackgroundColor3 = Theme.Accent
        end)

        local TabElements = { Page = PageFrame, Button = TabButton, Icon = TabIcon, Title = TabTitle }

        -- 🔘 1. عنصر الأزرار العامة (Button)
        function TabElements:AddButton(Text, Callback)
            Callback = Callback or function() end
            local Btn = Instance.new("TextButton")
            Btn.Size = UDim2.new(1, -6, 0, 34)
            Btn.BackgroundColor3 = Theme.ItemBg
            Btn.Text = Text
            Btn.TextColor3 = Theme.TextPrimary
            Btn.Font = Enum.Font.GothamBold
            Btn.TextSize = 11
            Btn.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Btn

            Btn.MouseButton1Click:Connect(function()
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.Accent}):Play()
                task.wait(0.1)
                TweenService:Create(Btn, TweenInfo.new(0.1), {BackgroundColor3 = Theme.ItemBg}):Play()
                Callback()
                NotifySystem(TitleText, "تم تفعيل: " .. Text, 2.5)
            end)
        end

        -- 🔘 2. عنصر مفاتيح التشغيل والإيقاف (Toggle)
        function TabElements:AddToggle(Text, Default, Callback)
            Callback = Callback or function() end
            local Toggled = Default or false

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 34)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(1, -40, 1, 0)
            Label.Position = UDim2.new(0, 10, 0, 0)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.TextPrimary
            Label.Font = Enum.Font.GothamBold
            Label.TextSize = 11
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame

            local Indicator = Instance.new("TextButton")
            Indicator.Size = UDim2.new(0, 18, 0, 18)
            Indicator.Position = UDim2.new(1, -26, 0.5, -9)
            Indicator.BackgroundColor3 = Toggled and Theme.Accent or Color3.fromRGB(40, 40, 50)
            Indicator.Text = ""
            Indicator.Parent = Frame

            local IndCorner = Instance.new("UICorner")
            IndCorner.CornerRadius = UDim.new(0, 4)
            IndCorner.Parent = Indicator

            local function UpdateToggle()
                Toggled = not Toggled
                Indicator.BackgroundColor3 = Toggled and Theme.Accent or Color3.fromRGB(40, 40, 50)
                Callback(Toggled)
                local Status = Toggled and "تم تشغيل: " or "تم إيقاف: "
                NotifySystem(TitleText, Status .. Text, 2.5)
            end

            Indicator.MouseButton1Click:Connect(UpdateToggle)
        end

        -- 🔘 3. عنصر شريط السحب والأرقام (Slider)
        function TabElements:AddSlider(Text, Min, Max, Default, Callback)
            Callback = Callback or function() end
            local Value = Default or Min

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 44)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(1, -50, 0, 18)
            Label.Position = UDim2.new(0, 10, 0, 4)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.TextPrimary
            Label.Font = Enum.Font.GothamBold
            Label.TextSize = 11
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame

            local ValLabel = Instance.new("TextLabel")
            ValLabel.Size = UDim2.new(0, 40, 0, 18)
            ValLabel.Position = UDim2.new(1, -45, 0, 4)
            ValLabel.BackgroundTransparency = 1
            ValLabel.Text = tostring(Value)
            ValLabel.TextColor3 = Theme.Accent
            ValLabel.Font = Enum.Font.GothamBold
            ValLabel.TextSize = 11
            ValLabel.Parent = Frame

            local SliderBar = Instance.new("Frame")
            SliderBar.Size = UDim2.new(1, -20, 0, 5)
            SliderBar.Position = UDim2.new(0, 10, 1, -10)
            SliderBar.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
            SliderBar.Parent = Frame

            local SliderFill = Instance.new("Frame")
            SliderFill.Size = UDim2.new((Value - Min)/(Max - Min), 0, 1, 0)
            SliderFill.BackgroundColor3 = Theme.Accent
            SliderFill.BorderSizePixel = 0
            SliderFill.Parent = SliderBar

            local Dragging = false
            local lastUpdate = 0

            local function Update(input)
                local now = tick()
                if now - lastUpdate < 0.02 then return end
                lastUpdate = now

                local pos = math.clamp((input.Position.X - SliderBar.AbsolutePosition.X) / SliderBar.AbsoluteSize.X, 0, 1)
                Value = math.floor(Min + (Max - Min) * pos)
                ValLabel.Text = tostring(Value)
                SliderFill.Size = UDim2.new(pos, 0, 1, 0)
                Callback(Value)
            end

            SliderBar.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = true; Update(input)
                end
            end)

            UserInputService.InputEnded:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    Dragging = false
                end
            end)

            UserInputService.InputChanged:Connect(function(input)
                if Dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                    Update(input)
                end
            end)
        end

        -- 🔘 4. عنصر صندوق النصوص (Text Input)
        function TabElements:AddTextbox(Text, Placeholder, Callback)
            Callback = Callback or function() end

            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 36)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(0.5, 0, 1, 0)
            Label.Position = UDim2.new(0, 10, 0, 0)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.TextPrimary
            Label.Font = Enum.Font.GothamBold
            Label.TextSize = 11
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame

            local InputBox = Instance.new("TextBox")
            InputBox.Size = UDim2.new(0.45, 0, 0.7, 0)
            InputBox.Position = UDim2.new(0.52, 0, 0.15, 0)
            InputBox.BackgroundColor3 = Color3.fromRGB(25, 27, 38)
            InputBox.Text = ""
            InputBox.PlaceholderText = Placeholder or "اكتب هنا..."
            InputBox.TextColor3 = Theme.Accent
            InputBox.PlaceholderColor3 = Theme.TextDark
            InputBox.Font = Enum.Font.GothamBold
            InputBox.TextSize = 10
            InputBox.Parent = Frame

            local BoxCorner = Instance.new("UICorner")
            BoxCorner.CornerRadius = UDim.new(0, 5)
            BoxCorner.Parent = InputBox

            InputBox.FocusLost:Connect(function(enterPressed)
                if enterPressed then
                    Callback(InputBox.Text)
                    NotifySystem(TitleText, "تم إدخال: " .. InputBox.Text, 2.5)
                end
            end)
        end

        -- 🔘 5. عنصر القوائم المنسدلة (Dropdown)
        function TabElements:AddDropdown(Text, ListOptions, Callback)
            Callback = Callback or function() end
            ListOptions = ListOptions or {}
            local Expanded = false

            local DropdownMain = Instance.new("Frame")
            DropdownMain.Size = UDim2.new(1, -6, 0, 36)
            DropdownMain.BackgroundColor3 = Theme.ItemBg
            DropdownMain.ClipsDescendants = true
            DropdownMain.Parent = PageFrame

            local DropCorner = Instance.new("UICorner")
            DropCorner.CornerRadius = UDim.new(0, 6)
            DropCorner.Parent = DropdownMain

            local HeaderBtn = Instance.new("TextButton")
            HeaderBtn.Size = UDim2.new(1, 0, 0, 36)
            HeaderBtn.BackgroundTransparency = 1
            HeaderBtn.Text = ""
            HeaderBtn.Parent = DropdownMain

            local Title = Instance.new("TextLabel")
            Title.Size = UDim2.new(1, -40, 1, 0)
            Title.Position = UDim2.new(0, 10, 0, 0)
            Title.BackgroundTransparency = 1
            Title.Text = Text
            Title.TextColor3 = Theme.TextPrimary
            Title.Font = Enum.Font.GothamBold
            Title.TextSize = 11
            Title.TextXAlignment = Enum.TextXAlignment.Left
            Title.Parent = HeaderBtn

            local Arrow = Instance.new("TextLabel")
            Arrow.Size = UDim2.new(0, 24, 0, 24)
            Arrow.Position = UDim2.new(1, -30, 0.5, -12)
            Arrow.BackgroundTransparency = 1
            Arrow.Text = "▼"
            Arrow.TextColor3 = Theme.Accent
            Arrow.Font = Enum.Font.GothamBold
            Arrow.TextSize = 10
            Arrow.Parent = HeaderBtn

            local ItemsHolder = Instance.new("Frame")
            ItemsHolder.Size = UDim2.new(1, -12, 0, #ListOptions * 28)
            ItemsHolder.Position = UDim2.new(0, 6, 0, 38)
            ItemsHolder.BackgroundTransparency = 1
            ItemsHolder.Parent = DropdownMain

            local ListLayout = Instance.new("UIListLayout")
            ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
            ListLayout.Padding = UDim.new(0, 4)
            ListLayout.Parent = ItemsHolder

            for _, opt in ipairs(ListOptions) do
                local ItemBtn = Instance.new("TextButton")
                ItemBtn.Size = UDim2.new(1, 0, 0, 24)
                ItemBtn.BackgroundColor3 = Color3.fromRGB(25, 27, 38)
                ItemBtn.Text = opt
                ItemBtn.TextColor3 = Theme.TextPrimary
                ItemBtn.Font = Enum.Font.GothamBold
                ItemBtn.TextSize = 10
                ItemBtn.Parent = ItemsHolder

                local ItemCorner = Instance.new("UICorner")
                ItemCorner.CornerRadius = UDim.new(0, 4)
                ItemCorner.Parent = ItemBtn

                ItemBtn.MouseButton1Click:Connect(function()
                    Expanded = false
                    Arrow.Text = "▼"
                    TweenService:Create(DropdownMain, TweenInfo.new(0.25, Enum.EasingStyle.Quart), {Size = UDim2.new(1, -6, 0, 36)}):Play()
                    Callback(opt)
                    NotifySystem(TitleText, "تم تحديد: " .. opt, 2.5)
                end)
            end

            HeaderBtn.MouseButton1Click:Connect(function()
                Expanded = not Expanded
                Arrow.Text = Expanded and "▲" or "▼"
                local TargetHeight = Expanded and (42 + #ListOptions * 28) or 36
                TweenService:Create(DropdownMain, TweenInfo.new(0.25, Enum.EasingStyle.Quart), {Size = UDim2.new(1, -6, 0, TargetHeight)}):Play()
            end)
        end

        -- 🔘 6. عنصر الكروت والملاحظات النصية (Label Banner)
        function TabElements:AddLabel(Text)
            local Frame = Instance.new("Frame")
            Frame.Size = UDim2.new(1, -6, 0, 30)
            Frame.BackgroundColor3 = Theme.ItemBg
            Frame.Parent = PageFrame

            local Corner = Instance.new("UICorner")
            Corner.CornerRadius = UDim.new(0, 6)
            Corner.Parent = Frame

            local Label = Instance.new("TextLabel")
            Label.Size = UDim2.new(1, -12, 1, 0)
            Label.Position = UDim2.new(0, 6, 0, 0)
            Label.BackgroundTransparency = 1
            Label.Text = Text
            Label.TextColor3 = Theme.Accent
            Label.Font = Enum.Font.GothamBold
            Label.TextSize = 10
            Label.TextXAlignment = Enum.TextXAlignment.Left
            Label.Parent = Frame
        end

        table.insert(WindowObj.Tabs, TabElements)

        if #WindowObj.Tabs == 1 then
            PageFrame.Visible = true
            TabTitle.TextColor3 = Theme.TextPrimary
            TabButton.BackgroundColor3 = Theme.Accent
        end

        return TabElements
    end

    -- ============================================================
    -- ⚙️ قسم الإعدادات وأدوات تسريع اللعبة الفعالة
    -- ============================================================
    local SettingsTab = WindowObj:CreateTab("الإعدادات", DeveloperLogo)

    SettingsTab:AddLabel("👑 MAX TITAN ENGINE - أدوات تسريع الفريمات والأداء")

    SettingsTab:AddButton("تنظيف الذاكرة العشوائية (RAM Cleaner) 🧹", function()
        pcall(function()
            collectgarbage("collect")
        end)
    end)

    SettingsTab:AddButton("تسريع الفريمات (Ultra FPS Boost) ⚡", function()
        pcall(function()
            for _, obj in ipairs(game:GetDescendants()) do
                if obj:IsA("BasePart") then
                    obj.Material = Enum.Material.SmoothPlastic
                elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") then
                    obj.Enabled = false
                elseif obj:IsA("PostEffect") or obj:IsA("BloomEffect") or obj:IsA("SunRaysEffect") or obj:IsA("BlurEffect") then
                    obj.Enabled = false
                end
            end
            Lighting.GlobalShadows = false
            Lighting.Technology = Enum.Technology.Compatibility
        end)
    end)

    SettingsTab:AddButton("إزالة الضباب والظلال (Clear Fog) 🌫️", function()
        pcall(function()
            Lighting.FogEnd = 9e9
            Lighting.GlobalShadows = false
        end)
    end)

    -- إشعار الترحيب المباشر
    NotifySystem(GetSmartGreeting(), "مرحباً بك يا " .. LocalPlayer.DisplayName .. " في " .. TitleText, 3.5)

    return WindowObj
end

return MAX_UI
